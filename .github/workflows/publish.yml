name: Publish update
on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  build:
    name: Build & publish
    env:
      DOCKER_TARGET_ACCOUNT: ${{ secrets.DOCKERHUB_TARGET }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v1
      - name: Enable experimental Docker features
        run: |
            mkdir -p ~/.docker
            echo "{\"experimental\": \"enabled\"}" > ~/.docker/config.json
      - name: Login to ghcr
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Convert GITHUB_REPOSITORY into lowercase
        run: |
          echo "REPO=${GITHUB_REPOSITORY,,}" >>${GITHUB_ENV}
      - name: Build for x86_64
        run: |
            docker build -t query-api .
      - name: Create manifest and push it
        if: env.DOCKER_USERNAME
        run: |
            docker tag query-api ${{ secrets.DOCKERHUB_TARGET }}/query-api
            docker push ${{ secrets.DOCKERHUB_TARGET }}/query-api
